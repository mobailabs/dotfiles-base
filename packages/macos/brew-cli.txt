# brew formulae（CLI 工具）。仓库只做 macOS，所以不再分 common / macos ——
# 这两份原先是 packages/common/brew-cli.txt 和 packages/macos/brew-cli.txt，
# 合并而来，顺序是「核心必需 → 日常增强」。

# ---- 核心必需 ----
zsh
eza
bat
git
tmux
neovim
fzf
ripgrep
mise
opencode
lazygit
trzsz-go
trzsz-ssh
jq

# ---- 日常使用 ----
gh
# xcodes CLI 属于 tap XcodesOrg/made，不在官方 core —— 必须写全路径。
XcodesOrg/made/xcodes

# ---- Zsh 增强 ----
# ⚠️ 两对竞品，每个都只能留一个（同时装两个会互相打架，且没被 source 的那个
#    属于「装了却永不生效」）：
#   zsh-autosuggestions           vs zsh-autocomplete      → 留 autosuggestions
#   zsh-syntax-highlighting       vs zsh-fast-syntax-highlighting → 留前者
# 被选中的在 src/macos/config/zsh/zshrc 里有对应 source 行；另一个不装。
zsh-autosuggestions
zsh-completions
zsh-syntax-highlighting
zsh-history-substring-search

# ---- 开发工具 ----
fd
direnv
git-lfs
gnu-sed
coreutils
findutils
moreutils

# ---- 监控 ----
bottom
htop
ncdu

# ---- 杂项 ----
wget
tree
zoxide
the_silver_searcher
oath-toolkit
ffmpeg
pngpaste
