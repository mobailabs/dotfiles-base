# SSH config
#
# 这个文件会被装到 ~/.ssh/config。**不要在这里放私钥。**
#
# 第一行的 Include 是「加载点」：它让 ~/.ssh/config.local 生效。
# 机器专属的 Host 段写进 ~/.ssh/config.local，不要写在这里 ——
# 这个文件是共享的，写进来会跟着仓库跑到别的机器上。
#
# 已验证：Include 一个不存在的文件会被 ssh 静默忽略（退出码 0），
# 所以这行即使本地还没有 .local 文件也不会报错。
#
# 想看最终生效的配置：
#   ssh -G some-host | head

Include ~/.ssh/config.local

# Host my-server
#   HostName 1.2.3.4
#   User ubuntu
#   IdentityFile ~/.ssh/id_ed25519
